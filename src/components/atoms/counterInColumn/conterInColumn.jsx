/* eslint-disable react/prop-types */
import React from "react";

const CounterInColumn = ({ counterColumn }) => (
  <h3 className="one-column-counter">{counterColumn}</h3>
);

export default CounterInColumn;
